/*
	10. Write a JavaScript program to calculate multiplication and division of two numbers (input from user).
*/

function compute(a, b) {
	var multiplicate = a * b; 
  	var divide =a / b;
  console.log("Multiplicate: " + multiplicate + "\n" + "Divide: " + divide)
}
compute(12, 4);